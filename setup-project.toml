# Slash Command: setup-project
name = "setup-project"
description = "Create or optimize README.md and AGENTS.md."
alias = ["setup"]

[options]
"project-name" = { type = "string", description = "The name of the project" }
"project-description" = { type = "string", description = "A brief description of the project" }
